# sleep-quality-predictor
SleepIQ — Sleep Quality Predictor
Quick Start (No server needed)
Just open index.html in your browser. Done!

VS Code Setup
Install "Live Server" extension in VS Code
Right-click index.html → "Open with Live Server"
File Structure
sleep-predictor/ ├── index.html ← Open this in browser ├── static/ │ ├── css/style.css ← All styles │ └── js/app.js ← All logic └── requirements.txt ← Python deps (for ML backend)
